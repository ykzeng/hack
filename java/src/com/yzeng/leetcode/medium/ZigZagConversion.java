package com.yzeng.leetcode.medium;

public class ZigZagConversion {
	public static String convert(String s, int numRows){
		int tot_col = s.length() / (2 * numRows - 2);
		//String result = "";
		char[][] result = new char[numRows][];
		
		
		for (int i = 0; i < numRows; i++) {
			
		}
	}
}
